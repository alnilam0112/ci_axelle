name: Deploy via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupération du code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Installer PHP et extensions
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, intl, pdo, xml, curl, zip

      # 3️⃣ Installer les dépendances PHP via Composer
      - name: Install PHP dependencies
        run: composer install --no-progress --no-interaction --prefer-dist

      # 4️⃣ Corriger les permissions sur les outils
      - name: Fix permissions
        run: |
          chmod +x ./vendor/bin/phpunit
          chmod +x ./vendor/bin/phpstan
          chmod +x ./vendor/bin/php-cs-fixer

      # 5️⃣ Lancer les tests unitaires avec PHPUnit
      - name: Run PHPUnit tests
        run: vendor/bin/phpunit

      # 6️⃣ Analyse statique avec PHPStan
      - name: Run PHPStan analysis
        run: vendor/bin/phpstan analyse

      # 7️⃣ Vérifier le style de code avec PHP CS Fixer
      - name: Run PHP CS Fixer
        run: vendor/bin/php-cs-fixer fix --dry-run --diff --allow-risky=yes

      # 8️⃣ Installer les dépendances Node.js si présentes
      - name: Install Node.js dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      # 9️⃣ Lancer les tests et lint JS
      - name: Run JS tests & lint
        run: |
          npm test
          npm run lint

      # 🔹 Vérifier les fichiers critiques
      - name: Check critical files
        run: |
          if [ ! -f ./config/.env ]; then
            echo "❌ Fichier .env manquant, arrêt du déploiement"
            exit 1
          fi

      # 🔹 Vérifier la branche
      - name: Ensure branch is main
        run:

